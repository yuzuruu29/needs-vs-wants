# Android / Gradle
.gradle/
build/
*.apk
*.aab
# Soft-launch site hosts public download APKs (Vercel upload respects gitignore)
!website/public/downloads/*.apk
!website/downloads/*.apk
local.properties
.idea/
*.iml

# Android SDK (committed locally, not needed in repo)
.android-sdk/

# Screenshots / design artifacts
*.png
*.jpg

# iOS build artifacts
ios/DerivedData/
ios/*.xcodeproj/
ios/*.xcworkspace/
ios/artifacts/

# OS
.DS_Store
Thumbs.db

.vercel

# Brainstorm / agent session artifacts
.superpowers/

# pre-reformat backup
.env
.env.local
.env.*.local

.hilo/

# Remotion promo project
video/node_modules/
video/out/
video/.remotion/
video/dist/
video/*.log

# Agent tooling / session dirs
.reasonix/
.zcode/
.commandcode/
.playwright-mcp/

# Generated graph caches
website/graphify-out/

# Python caches
docs/**/__pycache__/
*.pyc

# Local-only release signing (keystore + passwords) — never commit
.local/

# Cross-session heartbeat (overwritten every poll) — not tracked
.claude/sessions/status.md
