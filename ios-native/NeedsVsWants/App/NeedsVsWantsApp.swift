import SwiftUI

// P0 scaffold stub — replaced by the full app shell in Task P7.
@main
struct NeedsVsWantsApp: App {
    var body: some Scene {
        WindowGroup { Text("Needs vs. Wants — scaffold") }
    }
}
