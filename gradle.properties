org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Release signing: keystore path only (not a secret). The passwords live in
# ~/.gradle/gradle.properties (user home, NOT this repo) or env vars — see
# AGENTS.md "Release signing". Never commit release.keystore or passwords
# (the original one was public since 2026-07-29 and is BURNED — D86).
RELEASE_STORE_FILE=../.local/release.keystore
