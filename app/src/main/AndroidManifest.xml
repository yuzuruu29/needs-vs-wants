<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Supabase auth / entitlement remote calls (Task 3 seam; unused while SUPABASE_URL is blank) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- D195 frequency-tuned haptics through Vibrator composition primitives. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <application
        android:name=".NeedsVsWantsApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@drawable/app_icon_crop_center"
        android:roundIcon="@drawable/app_icon_crop_center"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.NeedsVsWants">
        <!-- Pre-download / declare ML Kit OCR on-device model dependency -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="ocr" />
        <!-- Sentry never auto-starts; CrashReporting.applyState() decides at
             runtime (release build + DSN configured + user opt-in). -->
        <meta-data android:name="io.sentry.auto-init" android:value="false" />
        <!-- AdMob App ID. TEST id by default (manifestPlaceholder fed from
             local.properties); the ads/ package + AdsConfig.ENABLED gate usage. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admobAppId}" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.NeedsVsWants"
            android:windowSoftInputMode="adjustResize"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- PayPal return / cancel deep links (subscription application_context) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="${deepLinkScheme}"
                    android:host="paypal" />
                <data android:path="/return" />
                <data android:path="/cancel" />
            </intent-filter>
            <!-- PayMongo return / cancel deep links (one-time manual-renewal checkout) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="${deepLinkScheme}"
                    android:host="paymongo" />
                <data android:path="/return" />
                <data android:path="/cancel" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".widget.NvwWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/nvw_widget_info" />
        </receiver>

        <!-- On-demand WorkManager via NeedsVsWantsApp (Configuration.Provider + HiltWorkerFactory). -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/receipt_file_paths" />
        </provider>
    </application>
</manifest>
