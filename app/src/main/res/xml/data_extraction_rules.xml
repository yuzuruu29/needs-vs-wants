<?xml version="1.0" encoding="utf-8"?>
<!--
    Android 12+ data extraction rules. The app declares
    android:allowBackup="false"; on Android 12+ that flag alone no longer stops
    device-to-device transfers, so every storage domain is excluded explicitly
    for BOTH cloud backup and D2D transfer. Nothing user-facing persists here
    beyond the Room entries database and DataStore preference files (which also
    hold the auth session and entitlement snapshot) — all of it stays on this
    device only.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
